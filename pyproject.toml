[project]
name = "price_tracker"
version = "0.0.1"

[tool.pytest.ini_options]
pythonpath = "custom_components/price_tracker"
addopts = [
    "--import-mode=importlib",
]

[tool.setuptools.packages]
find = { include = ["custom_components", "custom_components.*"] }
